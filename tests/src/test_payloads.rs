// tests/src/test_payloads.rs

pub fn generate_test_payloads() -> Vec<serde_json::Value> {
    vec![
        serde_json::json!({
            "prompt": "Animate a red circle moving from left to right across the screen"
        }),
        serde_json::json!({
            "prompt": "Create a sine wave graph that oscillates over time"
        }),
        serde_json::json!({
            "prompt": "Show a parabola curve y = x^2 being drawn from left to right"
        }),
        serde_json::json!({
            "prompt": "Animate the unit circle with a rotating radius line"
        }),
        serde_json::json!({
            "prompt": "Display the Pythagorean theorem with squares on each side of a right triangle"
        }),
        serde_json::json!({
            "prompt": "Create an animation of a 3D cube rotating on all axes"
        }),
        serde_json::json!({
            "prompt": "Show the Fibonacci spiral using golden rectangles"
        }),
        serde_json::json!({
            "prompt": "Animate a binary counter from 0 to 15 using 4 bits"
        }),
        serde_json::json!({
            "prompt": "Create a bar chart that updates values dynamically"
        }),
        serde_json::json!({
            "prompt": "Show the area under the curve for y = x^2 from 0 to 1"
        }),
        serde_json::json!({
            "prompt": "Animate the Taylor series approximation of sin(x)"
        }),
        serde_json::json!({
            "prompt": "Display the Mandelbrot set zooming into a specific region"
        }),
        serde_json::json!({
            "prompt": "Create an animation of a pendulum swinging with a trace"
        }),
        serde_json::json!({
            "prompt": "Show the solar system with planets orbiting the sun"
        }),
        serde_json::json!({
            "prompt": "Animate the Fourier transform of a square wave"
        }),
        serde_json::json!({
            "prompt": "Create a Voronoi diagram with moving seed points"
        }),
        serde_json::json!({
            "prompt": "Show the concept of limits approaching infinity"
        }),
        serde_json::json!({
            "prompt": "Animate the chain rule using function composition visualization"
        }),
        serde_json::json!({
            "prompt": "Display matrix multiplication as vector transformations"
        }),
        serde_json::json!({
            "prompt": "Create an animation of gradient descent on a loss surface"
        }),
        serde_json::json!({
            "prompt": "Show the central limit theorem with sampling distributions"
        }),
        serde_json::json!({
            "prompt": "Animate the Monte Carlo method for estimating pi"
        }),
        serde_json::json!({
            "prompt": "Create a 3D surface plot of a mathematical function"
        }),
        serde_json::json!({
            "prompt": "Show the concept of eigenvectors and eigenvalues"
        }),
        serde_json::json!({
            "prompt": "Animate the process of convolution between two functions"
        }),
        serde_json::json!({
            "prompt": "Display the heat equation on a 2D grid"
        }),
        serde_json::json!({
            "prompt": "Create an animation of a double pendulum chaos"
        }),
        serde_json::json!({
            "prompt": "Show the Lorenz attractor with a trailing path"
        }),
        serde_json::json!({
            "prompt": "Animate the construction of a fractal tree recursively"
        }),
        serde_json::json!({
            "prompt": "Create a visualization of prime numbers on a number spiral"
        }),
        serde_json::json!({
            "prompt": "Show the RSA encryption process step by step"
        }),
        serde_json::json!({
            "prompt": "Animate the PageRank algorithm on a small graph"
        }),
        serde_json::json!({
            "prompt": "Display the traveling salesman problem solution"
        }),
        serde_json::json!({
            "prompt": "Create an animation of quicksort on an array of bars"
        }),
        serde_json::json!({
            "prompt": "Show the process of training a neural network"
        }),
        serde_json::json!({
            "prompt": "Animate backpropagation through a small network"
        }),
        serde_json::json!({
            "prompt": "Create a visualization of word embeddings in 2D"
        }),
        serde_json::json!({
            "prompt": "Show the attention mechanism in transformers"
        }),
        serde_json::json!({
            "prompt": "Animate the convolution operation on an image"
        }),
        serde_json::json!({
            "prompt": "Display the batch normalization process"
        }),
        serde_json::json!({
            "prompt": "Create an animation of dropout regularization"
        }),
        serde_json::json!({
            "prompt": "Show the concept of vanishing gradients"
        }),
        serde_json::json!({
            "prompt": "Animate the greedy algorithm for coin change"
        }),
        serde_json::json!({
            "prompt": "Create a visualization of binary search tree insertion"
        }),
        serde_json::json!({
            "prompt": "Show the AVL tree balancing rotations"
        }),
        serde_json::json!({
            "prompt": "Animate Dijkstra's algorithm on a weighted graph"
        }),
        serde_json::json!({
            "prompt": "Display the A* pathfinding algorithm"
        }),
        serde_json::json!({
            "prompt": "Create an animation of BFS vs DFS on a grid"
        }),
        serde_json::json!({
            "prompt": "Show the merge sort algorithm with visual recursion"
        }),
        serde_json::json!({
            "prompt": "Animate the heap data structure insert and extract operations"
        }),
    ]
}